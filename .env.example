# --- Core server configuration ---
# Point this to your Postgres instance. Supabase users can copy the pooled connection
# string from Project Settings → Database and paste it here. Make sure it ends with
# ?sslmode=require so Drizzle can connect over TLS.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fitplanai

# Required for AI plan generation via OpenRouter.
OPENROUTER_API_KEY=sk-or-...

# Optional overrides for OpenRouter behaviour.
OPENROUTER_MODEL=openai/gpt-oss-120b:free
OPENROUTER_SITE_URL=https://fitplanai.local
OPENROUTER_APP_TITLE=FitPlanAI
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_TIMEOUT_MS=60000

# --- Supabase project metadata (optional but recommended when hosting on Supabase) ---
# These values come from Project Settings → API. Supply them to align the backend
# with your Supabase project credentials and to support future Supabase integrations.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
